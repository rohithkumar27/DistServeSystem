"""
GPU execution paths: real model prefill/decode (baseline vs disaggregated).

The files `src/simulator/baseline.py` and `disaggregated.py` are queueing *simulators*
using TimingModel formulas. For measured latencies, use `baseline_gpu` / `disaggregated_gpu`.
"""
